Go to DOGS_VS_ELEPHANTS directory in shell
Execute the following code in your shell: "Python mlp_training.py"
After the AI is trained, execute the following code to test it: "Python mlp_trial.py"
You may change the test images used in the folder.